<?php

namespace App\Models;


use Illuminate\Database\Eloquent\Model;

class Enquiries extends Model
{
    /*
     *  $table->increments('id');
            $table->string('name');
            $table->string('email');
            $table->string('mobile');
            $table->string('company');
            $table->text('message');
            $table->unsignedInteger('user_id')->nullable();

            $table->enum('status',['PENDING','RESOLVED','HOLD','OTHER']);

     */
}
